# Repo Lens samples

Hand-made inputs that trigger every check **at the default thresholds**, so manual
verification needs no settings changes. Each file targets one check and contains a
boundary case that must NOT be reported; the expected findings are tabulated in
[docs/manual-testing.md](../docs/manual-testing.md).

These files are not part of any Gradle source set and do not affect the build.
